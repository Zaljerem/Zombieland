# Zombieland 1.6 Porting Notes

This file is the single live coordination document for the RimWorld 1.6 port. Keep it compact: update it when a decision changes how future work should proceed, when a blocker is discovered, or when a repeatable test/build command changes.

## Work Cycle

1. Read `AGENTS.md` and this file before starting a new work unit.
2. Pick one narrow compile/runtime cluster and record the target in Current Unit.
3. Use `env -u RIMWORLD_MOD_DIR scripts/build-quiet.sh` for compile-only passes when the shell has a deploy directory exported. For live GABS testing on this machine, deploy with `RIMWORLD_MOD_DIR="$HOME/Library/Application Support/Steam/steamapps/common/RimWorld/RimWorldMac.app/Mods" scripts/build-quiet.sh`; the active local Zombieland root is inside the Steam app bundle, not the sibling `common/RimWorld/Mods` directory. Deploy builds now refuse to run while any `RimWorld by Ludeon Studios` process is alive, to avoid half-deleting the active mod folder. Do not paste full compiler logs into notes.
4. Use `scripts/validate-unity-assets.sh` before runtime tests that depend on Unity bundles. It loads deployed bundles with Unity `2022.3.62f3` from a temporary project copy.
5. Use `scripts/inspect-unity-assets.sh` when a runtime asset dependency is unclear. It logs deployed bundle asset types plus prefab component/material/shader details without modifying tracked Unity project state.
6. Use `scripts/export-unity-assets.sh` only after the missing source assets for the deployed bundle are recovered. It exports from a source-only temporary copy so the tracked Unity `Library` does not churn, and the Unity export method validates the complete deployed asset set before a bundle replacement can be accepted.
7. When runtime testing begins, summarize `Player.log` with `scripts/summarize-rimworld-log.sh` before inspecting raw logs.
8. Promote repeated manual RimWorld/GABS actions into scripts or saved scenarios. Use `scripts/run-zl-contract.sh` for focused Zombieland bridge contracts so GABS state, save loading, dynamic tool resolution, bridge readiness, operation events, and warning/error logs are captured the same way each time. Use `--verbose` only when the compact failure summary is not enough; full structured bridge payloads can get large.
9. For time-based runtime effects, derive the minimum tick window from Zombieland or RimWorld source before asserting. Advance that measured window instead of checking immediately after the trigger or guessing a long wait.
10. Revisit this file after each completed unit and prune stale notes instead of appending a new work log.

## Current Unit

- Target: continue focused RimWorld 1.6 runtime contracts for special-zombie, colony-interaction, and asset-backed behavior.
- Completed: Broad 1.6 port coverage is tracked by the `Latest verified` bullets below. Keep this line short; do not rebuild a long historical checklist here.
- Latest verified: `zombieland/zombie_extract_filter_visibility` covers the old broad zombie `ThingFilter.SetAllow` patch around serum harvesting: a fresh real `ThingFilter` can allow `ZombieExtract` and `ZombieSerumSimple`, cannot allow actual `Corpse_Zombie`/`Zombie` defs, and `ZombieSerumFilterWorker` does not exclude `ZombieExtract`.
- Latest verified: `zombieland/zombie_selection_respects_former_colonist` covers the selection UI contract around converted colonists: ordinary live zombies and ordinary zombie corpses are not map-click selectable, former-colonist zombies and their corpses are map-click selectable, the defensive `Selector.SelectInternal` patch still rejects ordinary zombie corpses, and former-colonist zombie labels keep the special green name color.
- Latest verified: `zombieland/zombie_social_thought_suppression` covers the quiet social-system exclusions: zombies cannot receive thoughts, relations/social-memory APIs return no relationship to normal colonists, social thought enumeration is empty, direct `Pawn_InteractionsTracker.TryInteractWith` is blocked in both directions, and observing a `ZombieCorpse` produces no mood thought or history event. This caught a 1.6 regression where the old interaction transpiler no longer blocked direct chitchat; the patch is now an explicit prefix and covers `Zombie`, `ZombieBlob`, and `ZombieSpitter`.
- Latest verified: `zombieland/zombie_corpse_alert_forbid_contract` covers the corpse-management edge around vanilla alerts and forbidding: normal and former-colonist zombie corpses are not treated as unburied colonist corpses and are not auto-forbidden outside the home area, while a real colonist corpse still is. The same fixture verifies zombie corpses remain valid for forced serum extraction and rejected by double-tap work.
- Latest verified: `zombieland/zombie_death_thought_suppression` covers the death-thought path itself: a real colonist death delivered `KnowColonistDied` to a generated observer as the positive control, then calling the same `PawnDiedOrDownedThoughtsUtility.TryGiveThoughts(..., Died)` path for a real adult zombie death added zero new memories.
- Latest verified: `zombieland/zombie_damage_memory_suppression` covers damage-time social memory suppression: human-on-human blunt damage created a real `HarmedMe` memory as the positive control, while otherwise equivalent zombie-on-human damage created zero new memories.
- Latest verified: `zombieland/zombie_health_needs_upkeep_suppression` covers zombie health/needs upkeep: a forced stale `Food` need is visible in a zombie's private needs list, the real patched `Pawn_NeedsTracker.AddOrRemoveNeedsAsAppropriate` clears it and rebinds direct need fields, `NeedsTrackerTickInterval(150)` does not advance a re-injected zombie need, and a source-derived one-day `ImmunityHandlerTickInterval(60000)` advances a human's plague immunity while leaving zombie immunity records at zero. The old needs transpilers are now explicit prefixes and the immunity guard covers `Zombie`, `ZombieBlob`, and `ZombieSpitter`.
- Latest verified: `zombieland/zombie_records_suppression` covers vanilla pawn records: a generated colonist mutates the raw and public `Kills` record from 0 to 3 through real `Pawn_RecordsTracker.Increment` and `AddTo`, while a real zombie remains raw/public zero after the same calls. The old records transpiler is now an explicit prefix and record getters use the shared Zombieland pawn guard.
- Latest verified: `zombieland/zombie_clamor_suppression` covers zombie-originated vanilla clamors: the generic `GenClamor.DoClamor(..., ClamorEffect)` path reaches a nearby listener for an ordinary human source, but normal zombie, spitter, and blob sources all produce zero listener effects. The clamor patch now targets all `GenClamor.DoClamor` overloads and uses the shared Zombieland pawn guard; direct `Pawn.HearClamor` keeps the same guard for typed clamor calls.
- Latest verified: `zombieland/tar_slime_move_cost_contract` covers the tar-slime movement-cost patch: a real `TarSlime` cell drives the private `Pawn_PathFollower.CostToMoveIntoCell(Pawn, IntVec3)` path to the Zombieland formula for ordinary humans, normal zombies, and zombie spitters. Spitters now use the same Zombieland pawn guard as normal zombies instead of falling through the non-zombie branch.
- Latest verified: `zombieland/zombie_blood_filth_contract` covers the zombie blood setting and tanky armor exception. RimWorld 1.6 removed the old `RaceProperties.BloodDef` flesh fallback, so Zombieland now declares `bloodDef=Filth_Blood` explicitly on normal zombies, blobs, and spitters. With `zombiesDropBlood=true`, humans, normal zombies, and spitters drop blood while tanky armor suppresses it; with the setting false, humans still drop blood and normal zombies/spitters do not. The old blood-drop transpiler is now an explicit prefix using the shared Zombieland pawn guard.
- Latest verified: `zombieland/tar_slime_fire_spread_contract` covers the `Fire.DoFireDamage` tar spread postfix: invoking the real private fire-damage method on burning `TarSlime` raises a tiny source fire to the 0.5 minimum and starts a real adjacent fire on neighboring `TarSlime`.
- Latest verified: `zombieland/tar_slime_fire_smoke_contract` covers the visible smoke path from a real burning `TarSlime`: the fixture advances the source-derived 15-tick fire update window and verifies the fire emits Zombieland `TarSmoke` at the burn cell instead of only vanilla white fire smoke. The `Fire.SpawnSmokeParticles` patch now suppresses the vanilla smoke fleck only for fires on `TarSlime`, while preserving normal fire glow.
- Latest verified: `zombieland/zombie_fire_watcher_excludes_attached_fires` covers the old burning-zombie fire-danger patch with a 1.6-safe prefix: `FireWatcher.UpdateObservations` counts a normal map fire and a human attached fire, but excludes attached fires on normal zombie, spitter, and blob pawns. Reloading `ZombieTest` from an already loaded map also stays log-clean because `ZombieTicker.DoSingleTick` now skips long-event/load windows before map components are fully ready.
- Latest verified: `zombieland/zombie_damage_log_association_suppression` covers the combat-log guard next to the fire patches. A human positive control gets `DamageResult.AssociateWithLog` combat text on a real injury hediff, while normal zombie, spitter, and blob injury hediffs keep no combat-log text. The old `hitThing is not Zombie` guard now uses the shared Zombieland pawn guard so spitter/blob damage-result association is suppressed too.
- Latest verified: `zombieland/zombie_fire_attachment_state_contract` covers the cached fire-state hook used by zombie fire logic: real `FireUtility.TryAttachFire` flips a normal zombie from no attachment/`isOnFire=false` to attached fire/`isOnFire=true`, and real `CompAttachBase.RemoveAttachment` clears both attachment state and `isOnFire`.
- Latest verified: `zombieland/zombie_fire_rain_vulnerability_contract` and `zombieland/zombie_fire_damage_reduction_contract` cover the zombies-burn-longer hooks for normal zombies, spitters, and blobs. The real private `Fire.VulnerableToRain` method stays vanilla for attached human fire, returns deterministic protected/unprotected mixes for attached Zombieland pawn fires while `zombiesBurnLonger=true`, and returns vanilla again when the setting is disabled. Real `Fire.DoFireDamage` at source max fire size still damages normalized human controls, while setting-on totals are lower than setting-off totals for normal zombie, spitter, and blob fixtures. Both hooks now use the shared Zombieland pawn guard.
- Latest verified: `zombieland/active_electrifier_attack_verb_contract` covers the attack-verb gate around active electrifiers: a drafted colonist with a real bolt-action rifle keeps a ranged `Verb_Shoot` against a normal zombie, but `Pawn.TryGetAttackVerb` resolves to a melee blunt verb against an active electrifier, matching `CanHarmElectricZombies` and the old no-ordinary-ranged-fire rule.
- Latest verified: `zombieland/active_electrifier_bullet_absorption_contract` covers the paired damage filter: a real active electrifier takes zero ordinary bullet damage and queues absorb-animation entries, while a matching disabled electrifier takes normal reduced zombie injury from the same bullet `DamageInfo`.
- Latest verified: `zombieland/active_electrifier_melee_shock_contract` covers the electrifier melee path: real `Pawn_MeleeVerbs.GetUpdatedAvailableVerbsList` hides `ZombieBite` for active and disabled electrifiers, and the private `Verb_MeleeAttackDamage.DamageInfosToApply` path converts an active electrifier's smokepop-belt target hit into Zombieland `ElectricalShock`/`ElectricalField` while a disabled electrifier stays ordinary scratch damage.
- Latest verified: `zombieland/albino_melee_bite_hidden_contract` covers the other half of the special-melee filter: real albino melee verb selection hides `ZombieBite` and uses scratch verbs, while a normal zombie positive control still exposes and may select `ZombieBite`. The bridge fixture snapshots the returned verb lists immediately because RimWorld 1.6 reuses a static list inside `Pawn_MeleeVerbs`.
- Latest verified: `zombieland/zap_zombies_with_shocker` builds a sealed wall room with a `ZombieShocker`, battery, conduit network, actor, and live zombie, then runs the real `ZapZombies` job. The source-derived window is 90 job-wait ticks plus the 45-tick `SubEffecter_ZombieShocker` windup plus one tick per standable room cell; the fixture hit at tick 154, spawned 16 zap motes, consumed battery energy, and set `zombie.paralyzedUntil`. The fixture must keep the battery footprint out of the wall cells or the room becomes invalid.
- Latest verified: `zombieland/thumper_impact_cycle` spawns a real `ZombieThumper`, fuels it with Chemfuel through `CompRefuelable.Refuel`, enables the switchable comp, runs the source-derived cycle at minimum nonzero intensity, and verifies impact by fuel consumption and `lastImpactTicks` advancing. The fixture hit impact at tick 105 with `intervalTicks=100`, `upTicks=9`, `fallTicks=5`, `radius=2`, and consumed 1 fuel. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_thumper_impact_cycle__cell_rect.png`. Avoid large thumper intensity in synchronous bridge tests on jungle maps because the seismic dust wave can damage many plants and blow up test time; use separate narrow tests for full-radius wave damage/infestation-repel behavior.
- Latest verified: `zombieland/chainsaw_equip_toggle` spawns and fuels a real `Chainsaw`, equips it on a generated drafted colonist after clearing existing zombies and default equipment, starts it through the chainsaw gizmo action, advances real game ticks so `Pawn_EquipmentTracker.EquipmentTrackerTick` drives `Chainsaw.Tick`, verifies fuel consumption while running, and verifies the `Pawn_DraftController.Drafted` patch stops the motor on undraft. Do not call `SetFactionDirect` on chainsaws; RimWorld logs an error because the weapon cannot have a faction. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_chainsaw_equip_toggle__cell_rect.png`.
- Latest verified: `zombieland/chainsaw_slaughter_zombie` uses the same real fueled/equipped chainsaw setup, starts it through the gizmo, places a live normal zombie in the target adjacent cell, aligns the chainsaw angle as fixture setup, and advances real game ticks until `Chainsaw.Tick` reaches `Slaughter`. It killed the zombie on tick 1, queued one `VictimHead`, consumed fuel, and reduced chainsaw hit points by 1 without breaking it. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_chainsaw_slaughter_zombie__cell_rect.png`.
- Latest verified: `zombieland/chainsaw_hits_building_contract` covers the other active chainsaw work branch. The fixture equips and starts a real fueled `Chainsaw`, keeps a live zombie adjacent so the chainsaw stays in the work branch, aims the blade at an adjacent wooden wall, and advances one real equipment tick. The wall took 80 crush damage, the chainsaw consumed fuel, dropped from equipment, lost 20 hit points, became broken, and was registered by `BrokenManager` while the nearby zombie stayed alive.
- Latest verified: `zombieland/chainsaw_crowd_pressure_drop_contract` covers the crowd-pressure safety branch. The fixture equips and starts a real fueled `Chainsaw`, places four hostile zombies in alternate adjacent cells around the drafted carrier, advances one real equipment tick, and verifies the chainsaw drops, stops, clears its pawn reference, stays unbroken, and leaves the pressure zombies alive.
- Latest verified: `zombieland/fix_broken_chainsaw_job` breaks a spawned `Chainsaw`, adds it to `BrokenManager`, provides one `ComponentIndustrial`, and runs the real `WorkGiver_FixBrokenChainsaw`/`JobDriver_FixBrokenChainsaw` path. The job completed at tick 1000, consumed the component, cleared the broken comp, and removed the chainsaw from `BrokenManager`. `WorkGiver_FixBrokenChainsaw` now permits factionless chainsaws; faction ownership must not be a hard rejection because chainsaw faction state is not reliable across spawned/equipped weapon states. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_fix_broken_chainsaw_job__cell_rect.png`.
- Latest rechecked: after the chainsaw tick allocation pass, all five chainsaw contracts pass through live GABS on RimWorld 1.6: equip/start/undraft stop, adjacent zombie slaughter, building impact/drop/broken tracking, crowd-pressure drop/stop, and component repair. `Chainsaw.Tick` now reuses two eight-slot buffers and avoids per-equipment-tick LINQ/list allocation while preserving the same adjacent-cell behavior. The same run left the RimWorld log summary clean.
- Latest verified: `zombieland/spawn_spitter_visual_fixture` covers the spitter's custom three-layer visual draw without launching a `ZombieBall`. RimWorld 1.6 was drawing the pawn render-tree `Spitter/Spitter.png` composite body in addition to `ZombieSpitter.DrawAt`'s manual `Spitter-back`/`Spitter-middle`/`Spitter-front` layers, which made one static front/body layer overlap the moving front/back panels. `PawnRenderer.RenderPawnAt` now skips `ZombieSpitter` so only the intended custom layers render. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_spitter_idle_visual_no_vanilla_body__cell_rect.png`; `zombieland/spit_zombie_ball` was rechecked on a fresh spitter after the render fix.
- Latest verified: `zombieland/damage_dark_slimer` now renders dense black tar smoke in live RimWorld 1.6 after moving `TarSmoke` to `drawerType=RealtimeOnly` with the mote shader. The previous bridge smoke only proved the `TarSmoke` gas existed; the normal map-mesh print path could still show the old gray/white texture. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_dark_slimer_tar_smoke_realtime_full.png`.
- Latest verified: `zombieland/tar_smoke_blocks_ranged_targeting` equips a generated colonist with a real bolt-action rifle, verifies the rifle can target a dark slimer before smoke, damages the dark slimer to create real `TarSmoke`, and verifies the same ranged verb can no longer target the zombie while it stands in tar smoke. `zombieland/tar_smoke_blocks_human_ranged_targeting` verifies the same obstruction behavior for an ordinary human target standing in direct `TarSmoke`, matching the def description that dense black smoke makes things hard to see. This also caught and fixed the 1.6 `ShotReport` regression where tar smoke was no longer present in RimWorld's cover list; the hit-chance patch now checks the report target cell's gas directly and the smoke reduces `AimOnTargetChance_StandardTarget` to zero.
- Latest verified: `zombieland/sticky_goo_toxic_buildup_contract` covers StickyGoo's pawn-contact effect without relying on a toxic splasher death. Moving a generated colonist off a clean cell creates no toxic buildup, while moving the same pawn off one real `StickyGoo` filth applies the source-derived 0.0230066683 toxic buildup delta through the `Thing.Position` setter patch; bridge logs and Player.log stayed clean.
- Latest verified: `zombieland/mine_with_miner` and `zombieland/mine_with_miner_job` cover the miner zombie mining path both directly and through the real `JobDriver_Stumble` loop. Both fixtures spawn a fresh miner beside `MineableSteel`; the direct `ZombieStateHandler.Mine(..., allDirections:true)` call and the real Stumble job each damage the mineable from 1500 to 1482 hit points, advance `miningCounter` to 162, and leave bridge warning/error logs clean.
- Latest verified: `zombieland/move_tanky`, `zombieland/damage_tanky_armor`, and `zombieland/smash_with_tanky` cover tanky movement side effects and combat durability. A moved tanky leaves the expected pheromone trace, bullet damage is absorbed by armor by degrading the shield from 1.0 to 0.752475262 without ordinary health damage, and the real route-to-`AttackStatic` path destroys a wooden wall from 195 hit points to zero. Bridge logs and Player.log stayed clean.
- Latest verified: `zombieland/damage_albino`, `zombieland/scream_with_albino`, and `zombieland/hack_flickable_with_albino` cover the albino damage and sabotage branches. Ordinary bullet damage shows both blocked and allowed samples under the deterministic filter, explosive `Bomb` damage still applies, the real sabotage scream reaches the source-derived 40-tick pulse and forces a nearby colonist into `Vomit` with stun, and the hacking branch switches off a real flickable building after the 240-tick action window. The damage fixture now stops its bullet sample as soon as both filter outcomes are observed so the explosive branch is measured on a live albino; bridge logs and Player.log stayed clean.
- Latest verified: `zombieland/kill_toxic_splasher`, `zombieland/move_dark_slimer`, `zombieland/heal_wounded_zombie`, `zombieland/heal_wounded_zombie_tick`, `zombieland/emp_electrifier`, and `zombieland/electrify_powered_building` cover the remaining direct special-zombie side effects. Toxic splasher death created four `StickyGoo` filth in radius 8; dark slimer movement added one `TarSlime`; healer direct and real Every12 tick paths removed one wound and queued one heal effect; EMP damage disabled an active electrifier without ordinary damage; and the powered-building electrify path disabled the electrifier to the expected tick and started one fire on a real powered `StandingLamp`/`PowerConduit` fixture. Bridge logs and Player.log stayed clean.
- Latest verified: `zombieland/wall_push_over_wall_contract` builds a real one-wall fixture, primes the actual pheromone grid to the source-derived threshold, starts the real `Stumble` job, and samples the wall-push animation until completion. With `minimumZombiesForWallPushing=18`, the single-wall bonus requires 14 primed grid zombies; the run started the push on tick 1, reached `wallPushProgress=0.989999354` on tick 100, landed on tick 102, left the wall intact, and cleared the constructed roof at the destination. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_wall_push_over_wall_contract__cell_rect.png`.
- Latest verified: `zombieland/wall_push_gate_contract` covers the matching rejection gates with real one-tick `Stumble` samples. It verifies no wall push starts when wall pushing is disabled, below the pheromone threshold, no adjacent wall exists, multiple adjacent walls exist, the destination is not walkable, a rock roof covers the destination, a cached zombie occupies the destination, or the zombie's wall-push cooldown is still active. The same deployed assembly rechecked the positive over-wall contract after sharing the fixture helper.
- Latest verified: `zombieland/wall_push_warning_letter_contract` covers the player-facing warning branch after a real wall-push start. An otherwise identical outside-home wall created no `DangerousSituation` letter, while a home-area wall created exactly one `DangerousSituation` letter after the bridge fixture cleared the realtime throttle key.
- Latest verified: `zombieland/zombie_ball_in_flight` launches a real `ZombieBall`, derives the travel window from projectile distance and `SpeedTilesPerTick`, advances to the halfway tick, verifies that the projectile is still spawned, moved, not at target, and rotating, then advances through impact and verifies that the projectile is gone and a delivered zombie was spawned.
- Latest verified: `zombieland/zombie_eats_corpse_contract` covers the real `JobDriver_Stumble` corpse-eating branch for human and animal flesh plus the non-flesh rejection gate. With corpse eating disabled, a normal zombie beside a flesh corpse stays in `Stumble` without selecting an eat target or removing body parts; with corpse eating enabled, human and Muffalo corpse fixtures both wait the source-derived Fat-body `Constants.EAT_DELAY_TICKS / 4` window and remove corpse body parts at tick 301. A mech corpse remains unselected and unconsumed across the same full delay window. The fixture searches isolated cells, clears the local pheromone/zombie grid, forbids spawned corpses to prevent hauling interference, and keeps the Player.log summary clean.
- Latest verified: `zombieland/zombie_eats_downed_pawn_contract` covers the matching downed-pawn setting branch through a real `JobDriver_Stumble` tick loop for human and animal flesh plus the non-flesh rejection gate. With downed-pawn eating disabled, a downed human pawn is not selected or consumed; with it enabled, generated human and Muffalo targets are selected, held through the source-derived Fat-body 300-tick eating delay, and lose body parts at tick 301. A downed mech target, held down with vanilla anesthetic instead of flesh-only blood loss, remains unselected and unconsumed in the negative window. `AlienTools.IsFleshPawn` now defaults to `RaceProps.IsFlesh`, so ordinary vanilla animals follow the same flesh-eating eligibility path as humanlike pawns.
- Latest verified: `zombieland/flee_ignores_harmless_zombies` restores the old 1.4 `SelfDefenseUtility.ShouldFleeFrom` semantic on RimWorld 1.6's `FleeUtility.ShouldFleeFrom`: a normal nearby zombie remains a flee threat, but roped, paralyzed/confused, active electrifier, and albino zombies are filtered through `SeesZombieAsThreat` and no longer trigger flee.
- Latest verified: `zombieland/hostility_to_zombies_contract` covers the broader `GenHostility.HostileTo` rules behind zombie target selection and path-grid checks: active colony pawns remain hostile to zombies, enemy pawns and animals follow their respective Zombieland settings, and factionless human pawns return non-hostile without throwing for both `Thing/Thing` and `Thing/Faction` overloads.
- Latest verified: `zombieland/zombie_active_threat_count_contract` covers the hostile-count path itself: `GenHostility.IsActiveThreatTo(..., Faction.OfPlayer, ...)` returns false for normal zombies, zombie spitters, and zombie blobs, so all Zombieland pawn classes are excluded from vanilla active-threat counts and danger music. The same run rechecked `zombieland/hostility_to_zombies_contract` after the shared hostility patch.
- Latest verified: `zombieland/zombie_target_cache_excludes_specials` covers the cached hostile-target set used by map-exist and danger-music logic: after real `AttackTargetsCache.UpdateTarget` calls, `TargetsHostileToColony` excludes normal zombies, zombie spitters, and zombie blobs. The old alternate cache collection now patches the `TargetsHostileToColony` getter and filters all Zombieland pawn classes instead of only `Zombie`.
- Latest verified: `zombieland/colonist_avoidance_interrupts_job` generates a real `AvoidGrid` from a nearby tracking zombie, starts a non-forced colonist `Wait_Combat` job, lets `JobDriver.DriverTick` run, and verifies it interrupts into a player-forced `Flee` job whose destination is outside the avoid grid.
- Latest verified: `zombieland/workgiver_respects_avoid_grid` verifies work assignment gating around zombie danger: a non-forced `WorkGiver_DoubleTap` rejects an infected corpse positioned in the avoid grid while the actor is outside danger, and the same target still creates a forced `DoubleTap` command job.
- Latest verified: `zombieland/avoid_grid_blocks_door_and_danger` verifies the adjacent avoidance APIs that colonist jobs depend on: `DangerUtility.GetDangerFor` reports an avoided door cell as `Deadly`, `Building_Door.PawnCanOpen` rejects a non-drafted/non-forced colonist for the avoided door, and drafted or player-forced states still bypass the avoid-grid block.
- Latest verified: `zombieland/avoid_grid_interrupts_existing_path` starts a real non-forced `Goto` job, waits only until RimWorld creates a valid path, uses Zombieland's source-derived `curPath.Peek(4)` lookahead cell, places a tracking zombie off the path so the lookahead cell becomes avoided without vanilla pawn collision, and verifies `Pawn_PathFollower.NeedNewPath` flips from false to true.
- Latest verified: `zombieland/avoid_grid_costs_route_new_path` covers the 1.6 replacement for the old pathfinder cost transpiler. Zombieland now injects a per-request `PathRequest.IPathGridCustomizer` built from the existing avoid grid; the bridge smoke compared the same direct path before and after enabling avoidance, dropping from 11 avoided cells / 7523 avoid cost to 0 avoided cells / 0 avoid cost. The existing `zombieland/avoid_grid_interrupts_existing_path` smoke still passes after this hook, so new-path routing and current-path invalidation now cover separate parts of the same avoidance use case.
- Latest verified: `zombieland/zombie_manual_door_close_ignored` opens a real player door, sets `Building_Door.ticksUntilClose` to a sentinel, verifies `StartManualCloseBy` from a zombie leaves it unchanged, and verifies the same vanilla call from a colonist still schedules the door close.
- Latest verified: `zombieland/albino_does_not_hold_door_open` covers the `Building_Door.Tick` transpiler against RimWorld 1.6's real auto-close loop: a normal zombie standing in the doorway resets `ticksUntilClose` from 10 to 109, while an albino zombie in the same situation is ignored by the pawn-presence check and lets it count down to 9.
- Latest verified: `zombieland/fogged_door_spawns_room_zombies` builds a sealed 25-cell room, refogs only the interior, and calls RimWorld's real `FogGrid.Notify_PawnEnteringDoor`. A hostile pawn is the negative control and leaves the room fogged with zero new zombies; a player pawn then triggers the Zombieland prefix before vanilla unfogging, spawning two wandering zombies inside the room and leaving the interior unfogged. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_fogged_door_spawns_room_zombies_final__cell_rect.png`.
- Latest verified: `zombieland/fog_blocker_removal_spawns_room_zombies` builds a sealed 25-cell fogged room, leaves one adjacent exterior cell unfogged, and destroys a real `Wall` with `DestroyMode.Deconstruct`. Zombieland's `Building.DeSpawn` prefix spawned two wandering zombies inside the fogged room before RimWorld's `FogGrid.Notify_FogBlockerRemoved` unfogged the interior. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_fog_blocker_removal_spawns_room_zombies__cell_rect.png`.
- Latest verified: `zombieland/fog_blocker_replacement_does_not_spawn_room_zombies` covers the negative branch for normal wall replacement: destroying the same fog-making wall with `DestroyMode.WillReplace` spawned zero zombies and left all 25 interior cells fogged. The room flag itself becomes false after the replacement hole connects to exterior space, so future fog-room tests should assert cell fog for replacement cases rather than relying on `Room.Fogged`. Screenshot evidence is in `~/Library/Application Support/RimWorld/Screenshots/zl_fog_blocker_replacement_no_room_zombies__cell_rect.png`.
- Latest verified: `zombieland/suicide_bomber_countdown_contract` covers the timed armed-bomb path through the real `Stumble` job. With `bombTickingInterval=1`, an unarmed suicide bomber stays alive and queues no explosion across 12 full game ticks, while an armed bomber dies on the countdown path on the next source cadence and the full tick drains `TickManager.explosions` immediately. The existing `zombieland/detonate_suicide_bomber` contract was rechecked after this and remains the direct `Zombie.Kill` queue handoff test: kill queues one explosion and `ExecuteExplosions` drains it.
- Latest verified: `scripts/export-unity-assets.sh`, `scripts/validate-unity-assets.sh`, and `scripts/inspect-unity-assets.sh` now cover the full Unity bundle loop under Unity `2022.3.62f3`: generated Win64/Linux/MacOS bundles contain `Dust`, `Metaballs`, `smoke_n.png`, `smoke_thin.mat`, `smoke_thin.png`, `zombieblob.mat`, and `zombieblob.shader`; validation loads all seven assets from each bundle; inspection confirms `Dust` is a billboard `ParticleSystem` using `Smoke_thin` with Unity's built-in `Particles/Standard Surface`, while the only custom shaders are `Custom/Metaballs` and `Custom/ZombieBlob`. The generated bundles replaced tracked `Resources`, the deployed mod path has matching hashes, `ZombieTest` loaded to visual readiness, `zombieland/spawn_blob` confirmed the new `Custom/Metaballs` asset loads, and `zombieland/thumper_impact_cycle` still passed with `intervalTicks=100`, `upTicks=9`, `fallTicks=5`, `radius=2`, and one fuel consumed at impact.
- Latest verified: the Unity bundle export was rerun from the repo `Originals/Effects` source through Unity `2022.3.62f3`; `Resources` now hashes to Linux `3b7442a7a77cfbd9dab21de447f81f721080b98bc3bee06291ad6fdbee5aeb44`, MacOS `ba4174c17e988a6ee243b478637976c93b45dc5e28d718ca7e91b4ad3f720a99`, and Win64 `fc6ae766420c252870e086ef58d039ee62a6d1217210a8f67af67c608c7f50bd`. `scripts/validate-unity-assets.sh` and `scripts/inspect-unity-assets.sh` both passed; the normal build deployed matching hashes to the Steam mod folder; after a GABS restart, `ZombieTest` loaded visual-ready, `zombieland/spawn_blob` returned `hasMetaballShader=true`, `zombieland/thumper_impact_cycle` hit impact at tick 105 with one fuel consumed, and both bridge and Player.log summaries stayed clean.
- Latest verified: the 2026-05-29 GABS recheck starts the configured Steam RimWorld target with Harmony before Core, then loads `ZombieTest` to visual readiness with no mod-configuration warnings. `zombieland/suicide_bomber_countdown_contract` still passes the source-derived unarmed/armed countdown split, all three wall-push contracts still pass the over-wall animation, rejection gates, and home-area warning letter branch, and the rebuilt Unity bundles still satisfy `zombieland/spawn_blob` (`hasMetaballShader=true`) plus `zombieland/thumper_impact_cycle` (impact at tick 105, one fuel consumed). `scripts/build-quiet.sh` passed with zero warnings/errors and both bridge log journal plus `scripts/summarize-rimworld-log.sh` stayed clean.
- Latest verified: `zombieland/zombie_thought_bubble_materials_contract` covers the custom thought-bubble feedback shared by eating, sabotage, scream, and contamination behaviors. RimWorld 1.6 stores spawned motes in `RealTime.moteList` rather than the map thing lister, so the contract samples object identity in that realtime list, calls the production BRRAINZ helper plus direct Eating/Hacking/Raging bubble casts, and verifies each spawns one `ZombieThought` at the zombie cell with the expected `Custom/Cutout_*` icon material. `ZombieTest` loaded visual-ready through GABS and both bridge and Player.log summaries stayed clean.
- Latest verified: `zombieland/contamination_core_contract` covers the first contamination-system slice: real human pawns keep contamination storage, the custom need, the `ContaminationEffect` hediff, and effectiveness in sync across add, set, and clear; non-pawn contamination clamps to 100%; and a real `Thing.SplitOff` preserves contamination on both stack parts. This caught a stale-hediff bug where `SetContamination` and `ClearContamination` changed only stored contamination; the storage layer now updates/removes pawn hediffs and marks the current map drawer dirty for direct set/remove paths.
- Latest verified: `zombieland/contamination_cell_fire_contract` covers two related contamination hooks: calling real `Pawn_FilthTracker.Notify_EnteredNewCell` on contaminated ground gives a human pawn the source-derived `cell * cellFactor * enterCellAdd` gain and keeps need/hediff state synced; calling real `Fire.DoComplexCalcs` reduces both item and ground contamination by the configured fire-reduction amount.
- Latest verified: `zombieland/contamination_filth_leavings_contract` covers two transient replacement-style contamination paths: a contaminated human pawn calling real `Pawn_HealthTracker.DropBloodFilth` creates contaminated `Filth_Blood` through the shared filth factory hook, and a contaminated real `ShipChunk` passed through `GenLeaving.DoLeavingsFor` transfers the configured `leavingsTransfer` amount into the spawned slag/component leavings while reducing the source by the same total.
- Latest verified: `zombieland/contamination_stack_absorb_contract` covers the ordinary stack-merge path next to `SplitOff`: two real spawned steel stacks, 6 at 0.2 contamination and 4 at 0.8, merged through `Thing.TryAbsorbStack` into one 10-stack at the weighted 0.44 contamination.
- Latest verified: `zombieland/contamination_carry_tracker_contract` covers carried-item contamination through real `Pawn_CarryTracker.TryStartCarry` and `CarryHandsTickInterval`. The fixture despawns a contaminated Steel stack into a drafted colonist's hands, advances to the source patch's `thingIDNumber % 900` cadence, and verifies the live `carryEqualize` value moves contamination from the carried item into the pawn while the item remains held.
- Latest verified: `zombieland/contamination_clear_snow_contract` covers cleanup transfer through real `JobDriver_ClearSnowAndSand`. The fixture creates a contaminated full-depth snow cell, starts a real `ClearSnow` job, advances up to the source-derived `50 * snowDepth / GeneralLaborSpeed` work window, and verifies the interval action clears snow and transfers the configured `snowAdd` amount into the worker. RimWorld 1.6 batches the interval action, so the live fixture can complete before the nominal per-tick work count.
- Latest checked: `zombieland/contamination_clear_pollution_contract` is in place for `JobDriver_ClearPollution`: it creates a one-cell polluted/contaminated cleanup fixture, then verifies the worker receives `pollutionAdd` contamination and the spawned wastepack receives `wastePackAdd` contamination through the real cleanup job. The current Core/Harmony/RimBridge/Zombieland load order has no `Wastepack`/`ClearPollution` defs available, so the live run skips cleanly; keep this for a pollution-enabled configuration.
- Latest verified: `zombieland/contamination_building_install_contract` covers contamination across building replacement flows. A contaminated stool now transfers contamination into its minified thing before vanilla despawns it, then into a real install blueprint, then into the installed building; a contaminated reinstall source similarly transfers into the reinstall blueprint and final moved building. `ReplacementContaminationState` is the shared capture-before-replacement/apply-after-replacement helper for these paths so future replacement edges can avoid one-off source-map-loss fixes.
- Latest verified: `zombieland/contamination_smooth_wall_contract` and `zombieland/contamination_smooth_wall_reversion_contract` cover smoothable rock replacement through real `SmoothableWallUtility` calls. Natural granite smoothing now sets the smoothed wall's contamination exactly instead of adding to the same mineable-backed ground cell, and smoothed-wall reversion captures contamination from the adjacent wall that actually reverts rather than from the wall whose destruction triggered the check.
- Latest verified: `zombieland/contamination_rest_comfort_contract` covers the resting comfort cadence hook. The fixture derives the source patch's `thingIDNumber % 1000` cadence, keeps a generated colonist in a forced `Wait_Combat` job on contaminated ground, calls real `PawnUtility.GainComfortFromCellIfPossible`, and verifies the configured `restEqualize` amount moves from the ground grid into the pawn when the cadence hits.
- Latest verified: `zombieland/contamination_recipe_product_contract` covers the shared `TransferContamination` path for spawned sources creating unspawned products. A spawned contaminated Steel ingredient run through real `GenRecipe.MakeRecipeProducts` transferred the expected 0.4 contamination into an unspawned ComponentIndustrial product and left the ingredient with the expected 0.4 remainder; the manager now carries the source map index while adding contamination to unspawned transfer targets.
- Latest verified: `zombieland/contamination_nutrient_paste_contract` covers contaminated feedstock passing through a real powered nutrient-paste dispenser and hopper into the unspawned `MealNutrientPaste` product. The fix avoids a brittle dispenser IL rewrite by using the general split-stack contamination handoff, including `ThingWithComps.SplitOff`, and a scoped dispenser prefix/postfix that transfers the captured split feed into the returned meal.
- Latest verified: `zombieland/contamination_frame_construction_contract` covers the normal construction replacement path from frame to finished building. A real wooden wall `Frame` with contaminated WoodLog in `resourceContainer` ran `Frame.CompleteConstruction`; the frame/material were destroyed, a wooden wall was spawned, and contamination split by the configured `constructionAdd`/`constructionTransfer` factors between the final wall and worker exactly as the production transpiler intends.
- Latest verified: `zombieland/contamination_terrain_construction_contract` covers terrain construction replacement paths through real `Frame.CompleteConstruction`. A contaminated WoodLog resource inside a `Frame_WoodPlankFloor` transferred 0.62 contamination to the constructed `WoodPlankFloor` cell, and a contaminated WoodLog resource inside a `Frame_Bridge` transferred 0.74 contamination through the `SetFoundation` branch to the constructed bridge cell. The frame transpiler now routes `SetTerrain`, `SetFoundation`, and `SetTempTerrain` through the same ground-contamination helper instead of only covering ordinary floor terrain.
- Latest verified: `zombieland/contamination_zombie_death_contract` covers the death-source side of contamination: killing a real normal zombie through `Pawn.Kill` adds `zombieDeathAdd` to the death cell, while killing an ordinary colonist on a clean control cell leaves that cell uncontaminated.
- Latest verified: `zombieland/contamination_corpse_inner_pawn_contract` covers the corpse handoff side of death contamination. A real contaminated colonist killed through `Pawn.Kill` produced a real corpse whose `InnerPawn` remained the source pawn and whose stored contamination exactly matched the pawn's pre-death contamination through the `Corpse.InnerPawn` setter patch.
- Latest verified: `zombieland/contamination_mineable_yield_contract` covers mining yield contamination through real `Mineable.DestroyMined`. Contaminated ground under a spawned `MineableSteel` transferred the expected `destroyMineableAdd` amount to the yielded Steel; the production patch now uses the mineable's map index while creating unspawned yield things instead of relying on the new thing's unset map state.
- Latest verified: `zombieland/contamination_deep_drill_contract` covers deep-drill resource production through real `CompDeepDrill.TryProducePortion`. A spawned `DeepDrill` over an injected one-portion Steel deep resource produced a real Steel stack, consumed the deep-resource cell, and gave the output the live configured `deepDrillAdd` contamination amount.
- Latest verified: `zombieland/contamination_plant_stump_contract` covers plant-to-stump replacement through real `Plant.TrySpawnStump`. A full-grown contaminated `Plant_TreeOak` spawned a `ChoppedStump` at the same cell and transferred the expected `stumpTransfer` amount through the existing production transpiler and shared `TransferContamination` path.
- Latest verified: `zombieland/contamination_plant_harvest_contract` covers harvested plant products through the real `JobDef Harvest` / `JobDriver_PlantHarvest` path. A contaminated full-grown `Plant_Rice` produced `RawRice` with the expected `plantTransfer` contamination; the source-derived work-only bound is recorded separately from path/job overhead, and the source plant is expected to read zero after RimWorld collects/destroys it.
- Latest verified: `zombieland/contamination_plant_sow_contract` covers plant creation through the real `JobDef Sow` / `JobDriver_PlantSow` path. A generated colonist sows `Plant_Rice` on a temporarily forced soil cell; the new plant receives the expected ground `sowedPlantAdd` contribution and the immediate `sowingPawnEqualize` transfer from the pawn. Later pawn contamination can continue changing through ordinary cell-contact effects, so the contract records the first post-spawn equalization sample separately from the final job state.
- Latest verified: `zombieland/contamination_wild_plant_spawn_contract` covers natural wild-plant creation through real `WildPlantSpawner.SpawnPlant`. RimWorld 1.6 keeps the actual `GenSpawn.Spawn` call in `SpawnPlant`, not `CheckSpawnWildPlantAt`, so the old replacement target was stale; the hook is now an explicit postfix on `SpawnPlant`, and a contaminated soil cell produced a `Plant_Grass` with the expected `plantAdd` contamination.
- Latest verified: `zombieland/contamination_ambrosia_sprout_contract` covers the ambrosia incident branch. The fixture pre-contaminates the real `IncidentWorker_AmbrosiaSprout.CanSpawnAt` cells, runs `AmbrosiaSprout` through its real worker, and verifies all spawned `Plant_Ambrosia` receive the configured `ambrosiaAdd` contamination from their ground cells.
- Latest verified: `zombieland/contamination_roof_collapse_contract` covers roof-collapse leaving generation through real `RoofCollapserImmediate.DropRoofInCellPhaseOne`. A thick rock roof over a 0.67-contaminated cell spawned `CollapsedRocks` with matching contamination; the patch now sets the leaving's contamination to the ground value instead of adding it, avoiding double-counting for `Mineable` leavings where contamination is already represented by the ground grid.
- Latest verified: `zombieland/contamination_effect_manager_contract` covers the mental-effect registration path: contaminated human pawns are added to `TickManager.contaminationEffects`, forcing the first source-derived effect tick at 24% contamination starts `ContaminationStateForceRest`/`ContaminationJobForceRest` with the expected 750-tick recovery window, and clearing contamination removes the pawn from the effect manager. This fixed an inert-effect regression where pawn hediffs existed but no contaminated pawns were ever registered for effect ticking, plus an inverted factor bug that could make higher-contamination effect durations zero or negative.
- Latest verified: `zombieland/contamination_hallucination_contract` applies the real hallucination effect, initializes its `JobDriver_ContaminationHallucination` ghost/path, then advances the source-derived 30-tick movement loop. This caught RimWorld 1.6 constant think-tree overrides replacing contamination jobs with normal `GotoWander`; Zombieland now blocks those overrides only while one of its active contamination job drivers is running, while normal job expiry still applies.
- Latest verified: `zombieland/contamination_hoard_pather_failure_contract` covers the hoarding contamination job's 1.6 pather-failure edge. `ContaminationEffect.Hoarding` starts a real `ContaminationJobHoard` from an assigned bed placed inside the fixture room, then the contract invokes the real driver pather-failure callback and verifies it keeps the hoarding job alive, clears the failed target, and records that thing in `rejectedThings` instead of ending the mental-state job as `ErroredPather`. The shared think-tree guard now also recognizes contamination jobs during the short job-created/driver-null window before driver initialization. This was added after a broader temporary hoarding probe exposed a native stack-guard crash while waiting for the async pickup path to resolve; do not restore the broad probe without tighter tick caps and a source-derived path window.
- Latest verified: `zombieland/contamination_hoard_driver_flow_contract` covers the positive hoarding driver flow without waiting through the crash-prone async path window. It builds two distinct non-huge rooms, assigns a real player bed to the contaminated pawn, places high-stack Silver in the source room, applies `ContaminationEffect.Hoarding`, advances the real first job tick so `JobDriver_ContaminationHoard` initializes and selects the source item, advances the source-derived 30-tick think-tree window, then invokes the real arrival callbacks to verify pickup transitions to `moveToStorage` and drop places the Silver in the owned bedroom. Rechecked `zombieland/contamination_hoard_pather_failure_contract` after this flow contract; both passed and logs stayed clean.
- Latest verified: `zombieland/defensive_defaults_contract` covers three latent legacy throw sites with old or corrupt enum-style state: `BrainzThought.CurStageIndex` now returns stage 0, invalid `SettingsKeyFrame.Unit` values default to days, and invalid `AttackMode` values fall back to non-hostile in the real `GenHostility.IsActiveThreatTo` Harmony patch. The contract loaded `ZombieTest`, spawned a real normal zombie, temporarily set `attackMode=(AttackMode)999`, checked an outlander faction against the patched active-threat path, and passed with no bridge or Player.log warnings.
- Latest verified: `zombieland/incident_special_type_spawn_contract` covers the non-debug event-spawning core used by `ZombiesRising` and queued streamer/RimConnect zombie events. The fixture reflects the real private `SpawnEventProcess`, uses the production event spawn locator/spot selection, requests one `SuicideBomber`, `ToxicSplasher`, `TankyOperator`, `Miner`, `Electrifier`, `Albino`, `DarkSlimer`, `Healer`, and `Normal` zombie, and verifies each request spawns exactly one matching `Zombie` in the expected event `Emerging` state. This gives simple coverage to the alternate special-zombie entry point without relying on mouse debug actions or bridge spawn helpers.
- Latest verified: `zombieland/rimconnect_special_type_queue_contract` covers the public RimConnect/streamer helper handoff before the event coroutine runs. The fixture preserves the existing `TickManager.rimConnectActions` queue, calls `RimConnectSupport.SpawnZombies(1, "Bridge", type)` for every explicit special zombie event type, verifies the returned event spot and user-facing message, and inspects the queued action closure to confirm the requested `ZombieType` survived into the TickManager queue.
- Latest verified: `zombieland/rimconnect_rage_kill_contract` covers two direct streamer/RimConnect action methods without requiring the external RimConnect mod to be loaded. The fixture clears the live session's zombies, spawns two controlled cached zombies, calls `RimConnectSupport.AllZombiesRage` and verifies both receive future `raging` deadlines, then calls `RimConnectSupport.KillAllZombies` and verifies both are dead/unspawned with zero remaining spawned test zombies. The contract also checks the user-facing action messages and cleans up created pawns/corpses.
- Latest verified: `zombieland/rimconnect_super_drop_contract` covers the remaining streamer/RimConnect action method. The fixture calls `RimConnectSupport.SuperZombieDropRaid(2, "Bridge")`, verifies the user-facing message, valid raid spot, and two real `DropPodIncoming` things created by RimWorld's `DropPodUtility.DropThingsNear`, then destroys the generated drop pods. This exercises the unspawned-zombie/drop-pod handoff path without needing the external RimConnect mod active.
- Latest verified: `zombieland/incident_scheduling_contract` covers the zombie incident scheduler itself without spawning an event wave. The fixture temporarily supplies an armed capable colonist when `ZombieTest` has only incapable colonists, swaps in an isolated `IncidentInfo`, disables dynamic threat, then calls the real `ZombiesRising.ZombiesForNewIncident` for the source-derived free-days skip (`waiting for zombies`), empty-incident skip, and positive incident calculation. The live run produced a positive incident size of 15 from 20 calculated zombies, updated `lastIncident` to the current tick, restored the original scheduler/settings state, and `rimworld/list_colonists` confirmed the temporary pawn was cleaned up. A transient `Root_Play.Start` async NRE occurred on the first post-restart load attempt, but acknowledging it and retrying loaded `ZombieTest` visual-ready and the contract passed.
- Latest verified: `zombieland/incident_alert_wave_contract` covers the user-facing threat-letter branch in the event wave coroutine. The fixture runs the real private `ZombiesRising.SpawnEventProcess` with `useAlert=true`, `ignoreLimit=true`, and four normal zombies so the `zombiesSpawning > 3` branch fires. It verifies both `FromTheEdges` and `AllOverTheMap` spawn modes create four `Emerging` zombies and exactly one `ThreatSmall` letter with the expected translated labels (`Zombie attack` and `Direct attack of zombies`). The fixture cleans up spawned zombies, restores `spawnHowType`, and `Player.log` stayed clean.
- Latest verified: `zombieland/random_zombie_type_weights_contract` covers the natural settings-weighted `ZombieType.Random` route into `ZombieGenerator.PrepareZombieType`. The fixture forces one special-zombie chance to 1.0 at a time, then clears all special chances for the normal fallback, spawning through the same `ZombieRuntimeActions.SpawnZombie(..., ZombieType.Random, true)` path used by normal random creation. It verified Suicide Bomber, Toxic Splasher, Tanky Operator, Miner, Electrifier, Albino, Dark Slimer, Healer, and Normal all produce the expected flags/body setup, restores all chance settings, cleans up spawned zombies, and `Player.log` stayed clean.
- Latest checked: `zombieland/child_zombie_generation_contract` is in place for the source-derived `childChance` branch, but the current Core/Harmony/RimBridge/Zombieland load order has `BodyTypeDefOf.Child == null`, so the live run skipped cleanly. Keep this as the future fixture for a Biotech/child-enabled configuration: it verifies `childChance=1` creates child normal zombies while preserving adult Hulk/Fat body rules for suicide bombers and tanky operators.
- Latest verified: `zombieland/zombie_faction_world_contract` covers the loaded-world side of the zombie faction patches. On `ZombieTest` it found exactly one hidden, non-temporary `Zombies` faction; verified player-vs-zombie hostility in both directions; and verified all eleven non-zombie factions have mutual `Hostile` relations with the zombie faction. This supports the spawning/hostility contracts that assume `FactionUtility.DefaultFactionFrom(ZombieDefOf.Zombies)` resolves cleanly after save load.
- Latest verified: `zombieland/zombie_faction_pawn_generation_contract` covers the PawnGenerator prefix that redirects normal zombie faction generation into Zombieland's real spawn path while preserving direct pawn creation for spitter/blob kinds. The live run generated a normal `Zombie` for the `Zombies` faction, verified it spawned through the patch and appeared as exactly one new map zombie, and verified `ZombieSpitter` and `ZombieBlob` generation still returned unspawned special pawn instances. The fixture cleans up the generated pawns/cache entries and `Player.log` stayed clean.
- Latest verified: `zombieland/infected_incident_hooks_contract` covers the two incident infection postfixes. It reflects `PawnGroupKindWorker.GeneratePawns`' postfix with `infectedRaidsChance=1` and dynamic threat disabled, verifying a spawned human raid pawn receives a harmful zombie bite while a generated Muffalo does not. It then reflects `IncidentWorker.TryExecute`'s postfix against real `IncidentParms.pawnGroups`, verifying a final-stage bite becomes `BittenHarmless` and `mayBecomeZombieWhenDead=false`. A native RimWorld `FastTileFinder.RegenerateCache` load crash occurred once before the run; GABS restart/load retry succeeded and `Player.log` stayed clean afterward.
- Latest verified: `zombieland/zombie_ticking_budget_contract` covers reduced zombie ticking and movement compensation. The fixture fills the moving average to 25%, spawns 20 normal zombies, runs the production `TickManager.ZombieTicking()` path, and verifies exactly five live zombies are ticked and counted through `ZombieTicker.zombiesTicked`. It also verifies move-speed compensation remains active at the same 25% budget with a 4x speed ratio. `TickManager.ZombieTicking` and threaded ticking now increment `ZombieTicker.zombiesTicked`, and `PrepareThreadedTicking` uses a partial in-place sample instead of a full LINQ random-order pass for throttled horde ticks.
- Latest verified: `zombieland/zombie_ticking_feedback_contract` covers the adaptive tick-budget feedback loop around the real `TickManagerUpdate` patch. The prefix resets `ZombieTicker.zombiesTicked`, sizes max/current ticking from the live cache and current percent, and the postfix writes the expected sampled ratio back into the moving average; the verified run used 20 spawned zombies, read a 25% prefix budget, and reduced a 100/400 sample into the expected 0.25 slot and 0.90625 average.
- Latest rechecked: the ticking hot path now avoids LINQ predicate closures while counting live cached zombies and preparing the tick sample, and materializes the per-map `TickManager` list once per `TickManagerUpdate` prefix. `zombieland/zombie_ticking_budget_contract` and `zombieland/zombie_ticking_feedback_contract` both still pass after the loop cleanup, and the Player.log summary stayed clean.
- Latest verified: `zombieland/contamination_mimic_contract` applies the real mimic contamination effect, confirms it survives RimWorld's 30-tick think-tree pass, then advances until the real mimic driver reaches the target, records `previousVictims`, applies `ZombieScare`, and starts the target's `Flee` job. This caught a 1.6 pathing regression where `JobDriver_ContaminationMimic` restarted `StartPath` every tick and starved movement; it now preserves the current path and refreshes only when stopped or on the source-derived 30-tick retarget interval. The fixture also keeps the target stationary and isolated so the driver chooses naturally instead of the test forcing its victim.
- Latest verified: `zombieland/contamination_sleepwalk_contract` starts from a real sleeping pawn, creates a separate occupied colonist bed, applies `ContaminationEffect.Sleepwalk`, confirms the custom job survives RimWorld's 30-tick think-tree pass, then advances until the sleepwalker reaches the bed, wakes the occupant, and starts the occupant's `Flee` job. The fixture chooses cells away from existing `ZombieTest` pawns/zombies and outside the current avoid grid so normal zombie panic cannot satisfy or block the sleep precondition.
- Latest verified: `zombieland/contamination_breakdown_contract` applies the real breakdown contamination effect and confirms it immediately selects a flee path, moves, and survives RimWorld's 30-tick think-tree pass. This caught an inert driver bug where `JobDriver_ContaminationBreakdown.InitAction` did nothing and movement only started from path-arrival/failure callbacks that never fired for a fresh job; the driver now calls its existing `Flee()` routine during init.
- Latest verified: `zombieland/contamination_ingestion_contract` covers the food-ingestion transfer path through real `Thing.Ingested`. A contaminated five-stack packaged survival meal transferred exactly the source-derived partial-stack amount to the eater and kept the remaining stack contamination reduced by the same amount; in the live run, one meal transferred 0.096 contamination and left the remaining stack at 0.404.
- Latest verified: `zombieland/contamination_tending_contract` covers the medical transfer path through real `TendUtility.DoTend`. A contaminated two-stack industrial medicine treated a real cut, transferred the source-derived amount first to the patient and then from the remaining medicine to the doctor, and equalized doctor/patient contamination at Medicine skill 0 while keeping stored contamination, need level, and hediff severity in sync. The fixture uses a two-stack medicine item because vanilla tending consumes one medicine; this leaves the remaining thing alive so Zombieland's postfix can persist the reduced source contamination.
- Latest verified: `zombieland/zombie_area_risk_contract` covers the dangerous-area alert classifier behind the top-screen risk strip. `ColonistInside`/`ColonistOutside` flag only colonists, while `ZombieInside` and `ZombieOutside` now flag normal zombies, zombie spitters, and zombie blobs consistently. This caught a blind spot where the area manager read only `TickManager.allZombiesCached`, which excludes spitter/blob pawn classes.
- Latest verified: `zombieland/contamination_melee_equalize_contract` covers the real protected `Verb_MeleeAttack.ApplyMeleeDamageToTarget` path. A clean colonist melee-hit a 0.8-contaminated colonist through a real melee verb, real damage was applied, and contamination equalized by the configured `meleeEqualize` factor while both pawn need/hediff states stayed synced. `zombieland/contamination_clear_pollution_contract` remains the future pollution/Biotech fixture; the current Core/Harmony/RimBridge/Zombieland loadout still has no `Wastepack`/`ClearPollution`, so the live run skips cleanly. Source inspection of RimWorld 1.6 `JobDriver_ClearPollution` still found and fixed two real handoff issues: the worker subject now wraps the actual `tickIntervalAction` that calls `ClearPollutionAt`, and the spawned wastepack contamination now comes from the cleared job target instead of the possibly adjacent wastepack spawn cell.
- Latest verified: the corpse/infection bridge slice was rerun from a clean single GABS session after the overlapping-Codex-turn incident. `zombieland/extract_serum_from_zombie_corpse` now produces extract at tick 266 and keeps Player.log clean because `DescribeCorpse` no longer asks RimWorld for `Corpse.LabelCap` or `InnerPawn` on destroyed/bugged corpses. The same clean pass rechecked `zombieland/cure_zombie_infection_recipe`, `zombieland/double_tap_infected_corpse`, `zombieland/rope_zombie_job`, `zombieland/zombie_extract_filter_visibility`, `zombieland/brainz_thought_bubble_contract`, and `zombieland/zombie_thought_bubble_materials_contract`. `zombieland/convert_infected_corpse_to_zombie` is an action endpoint that requires an existing pawn identifier, not a self-contained contract.
- Latest verified: the special-zombie behavior slice now rechecks `zombieland/damage_dark_slimer`, `zombieland/kill_toxic_splasher`, `zombieland/heal_wounded_zombie_tick`, `zombieland/emp_electrifier`, `zombieland/electrify_powered_building`, `zombieland/sticky_goo_toxic_buildup_contract`, `zombieland/mine_with_miner_job`, `zombieland/suicide_bomber_countdown_contract`, and `zombieland/detonate_suicide_bomber` from a clean GABS session with a clean Player.log. `zombieland/detonate_suicide_bomber` now spawns a fresh suicide bomber when no target is supplied, making the direct kill/explosion-queue path self-contained like the countdown fixture.
- Latest verified: the wall-push behavior slice now rechecks `zombieland/wall_push_gate_contract`, `zombieland/wall_push_over_wall_contract`, and `zombieland/wall_push_warning_letter_contract` from a clean GABS session. The gate fixture covered eight source-derived rejection/acceptance cases, the over-wall fixture exercised the real `Stumble` progress window, and the warning fixture verified letters fire for home-area walls without firing for non-home walls. Player.log stayed clean.
- Latest verified: the avoidance/pathing/door slice now rechecks `zombieland/flee_ignores_harmless_zombies`, `zombieland/colonist_avoidance_interrupts_job`, `zombieland/workgiver_respects_avoid_grid`, `zombieland/avoid_grid_blocks_door_and_danger`, `zombieland/avoid_grid_interrupts_existing_path`, `zombieland/avoid_grid_costs_route_new_path`, `zombieland/zombie_manual_door_close_ignored`, and `zombieland/albino_does_not_hold_door_open` from a clean GABS session. This protects the earlier movement/jitter and avoid-grid risk area: existing paths request a new route when their lookahead becomes dangerous, new 1.6 path requests route through fewer zombie-danger cells, and door close/hold-open behavior remains zombie-aware.
- Latest verified: the asset-backed projectile/effect slice now rechecks `zombieland/spawn_blob`, `zombieland/spit_zombie_ball`, `zombieland/zombie_ball_in_flight`, `zombieland/impact_zombie_ball`, `zombieland/thumper_impact_cycle`, and `zombieland/thumper_visual_wave_damage_contract` from a clean GABS session. The thumper wave now uses the old 1.4 bundle-derived `Dust` prefab orientation and alpha ramp, but scales runtime particle speed from `Radius = MaxRadius * intensity`; because the prefab circle shape already starts at radius 2, `ShockwaveStartSpeed` travels `Radius - 2` over `impactDurationTicks` so the visible ring and damage wave share the same origin and endpoint. The generated `smoke_thin` texture has zero-alpha borders to hide plane clipping, and `ZombieThumper` keeps the dust object alive after damage completion so the tail can fade instead of being destroyed at the radius edge.
- Latest verified: `scripts/run-zl-contract.sh` can run focused Zombieland bridge contracts end-to-end through GABS: it loads `ZombieTest`, resolves mirrored tool names dynamically, checks bridge readiness, classifies GABS/attention/bridge-operation/contract failures, captures operation events, and prints compact warning/error evidence. Case-heavy contracts are compacted by samples; single-result tools now keep their full structured result instead of collapsing to an empty `cases` array. The multiple-RimWorld guards in this runner and `scripts/build-quiet.sh` now read executable `comm` names instead of full shell command lines, so wrapper commands mentioning the Player.log path or the RimWorld executable name no longer look like extra RimWorld processes. Use the runner for repeated contract checks instead of hand-assembling the same GABS JSON-RPC sequence.
- Latest bridge-analysis lesson: classify failures by layer before changing gameplay code. The recent eating-contract work separated Codex MCP transport trouble, GABS lifecycle/disconnects, RimBridge operation failures, contract assertion failures, and application exceptions. The useful gameplay failure was a factionless non-flesh/mech fixture triggering null-unsafe `AttackTargetFinder`/hostility patches; those patches now leave vanilla behavior in place when no faction exists, and bridge contracts keep operation events plus Player.log summaries attached to each run.
- Latest bridge-hardening pass: RimBridgeServer now falls back to `~/.gabs/rimworld/bridge.json` when Steam does not pass the GABS environment through, so GABS Steam launches no longer leave RimBridge listening standalone on port 5174. Annotated extension tools now have a bounded main-thread execution timeout, and `scripts/run-zl-contract.sh` passes `_rimBridgeTimeoutMs` just below its outer GABS timeout so contract stalls return as RimBridge operation timeouts instead of vague request-layer failures. The runner's attention probe now uses 10 seconds because a 2-second check was too sensitive immediately after `load_game_ready`; use `scripts/check-bridge-health.sh` before gameplay debugging when the bridge state is uncertain.
- Next blocker cluster: validate special zombie behaviors beyond visuals.

## Decisions

- Use `Krafs.Rimworld.Ref` `1.6.4633` as the stable 1.6 reference baseline. Avoid prerelease refs until there is a concrete reason.
- Older local RimWorld versions are available at `/Users/ap/Documents/OlderRimWorlds` (`RimWorldMac1.4.app`, `RimWorldMac1.5.app`, `RimWorldMac1.6.app`). Use them for decompiler comparisons when a 1.4 behavior needs to be preserved.
- Supported load folders are `v1.4` and `v1.6` only. Do not spend effort preserving RimWorld 1.5 compatibility. Current builds output to `1.6/Assemblies`; legacy tracked assemblies live under `1.4/Assemblies`.
- RimWorld 1.6 uses Unity `2022.3.35f1` on macOS. The local safe editor is `2022.3.62f3`; use it for Zombieland asset-bundle rebuilds. Current checked-in bundles were rebuilt by Unity `2022.3.62f3`; the previous bundles were built by Unity `2019.4.30f1`.
- Unity `2022.3.62f3` can rebuild the Zombieland bundles from `Originals/Effects` again. `CreateAssetBundles.BuildStandaloneAssetBundles` now generates the previously missing `_Zombieland` bundle-source assets at export time, labels them for the `zombieland` bundle, validates the complete seven-asset contract, and writes the platform bundles. The thumper's visible building layers are normal RimWorld textures, but its impact wave creates the bundle `Dust` prefab. `scripts/inspect-unity-assets.sh` verifies that `Dust` is a billboard `ParticleSystem` using material `Smoke_thin` with Unity's built-in `Particles/Standard Surface` shader; the custom shaders in the generated bundles are `Custom/Metaballs` and `Custom/ZombieBlob`, so there is no separate custom thumper shader to preserve.
- Preserve gameplay behavior while getting the project compiling. Runtime behavior changes need source evidence from RimWorld 1.6 or live testing.
- Prefer small compatibility shims/patch retargets over broad rewrites until the 1.6 API shape is confirmed.
- Use GABS `games_start`/`games_stop`/RimBridge tools for runtime testing. Do not fall back to direct app launches when GABS startup fails; fix the GABS path or stop and report the blocker.
- For repeatable Zombieland bridge contracts, prefer `scripts/run-zl-contract.sh <tool>` over one-off curl commands. It handles the current HTTP GABS fallback path, refuses multiple RimWorld processes, loads `ZombieTest` by default, gates on RimBridge automation readiness, resolves dynamic mirrored tool names, prints compact operation/log evidence around the call, and keeps full contract JSON behind `--verbose`.
- Use `scripts/check-bridge-health.sh` after GABS reconnects, contract timeouts, or confusing transport failures. It checks GABS state, blocking attention, `rimbridge/get_bridge_status`, and the cheap `zombieland/get_status` extension path without changing gameplay state.
- Use native 1.6 saves for runtime fixtures. Do not load RimWorld 1.4 saves in 1.6; use `/Users/ap/Documents/OlderRimWorlds/RimWorldMac1.4-UserData/Saves/Test.rws` only as a visual/source reference.
- GABS process matching can be confused by a simultaneously running older RimWorld process because the executable name is still `RimWorld by Ludeon Studios`. Confirm the active 1.6 GABS connection before runtime tests.
- `Source/BridgeTools/` exposes repeatable Zombieland bridge actions as topic partials of `ZombielandBridgeTools`. Prefer `zombieland/spawn_reference_lineup` over repeated manual debug-action/UI spawning for the eight-type visual fixture. Debug actions and bridge endpoints share `Source/ZombieRuntimeActions.cs` for zombie spawn/remove, bite/infection, and direct pawn conversion operations; keep future test-only runtime mutations there instead of duplicating menu and bridge logic.
- Bridge fixtures that disable generated pawn work should use `DisablePawnWork` so RimWorld initializes `Pawn_WorkSettings` before `DisableAll`; direct `workSettings?.DisableAll()` can log fixture-only errors for some generated pawns.
- Bridge tools are split by topic under `Source/BridgeTools/` as partial `ZombielandBridgeTools` files. Keep shared map, tick, settings, cleanup, and description helpers in `ZombielandBridgeTools.Common.cs` or another narrow shared helper so bridge contracts and future RimBridge actions do not grow another monolithic file.
- Fog-room bridge smokes share the small `TryBuildFogRoomFixture` helper. Keep new sudden-room-zombie tests on that helper unless they need a materially different room topology.
- The in-game bottom-right Menu tab uses `ListableOption_Zombieland` for the Zombieland settings option. It draws `ZombieButtonBackground` directly at the option level; avoid reviving the old global `Widgets.ButtonTextWorker` texture transpiler.
- Dark slimer damage must spawn Zombieland `TarSmoke` directly instead of routing through `GenExplosion` with `DamageDefOf.Smoke`; the explosion path can layer vanilla white smoke puffs over the black tar gas in RimWorld 1.6. `TarSmoke` must use `drawerType=RealtimeOnly` so RimWorld calls `TarSmoke.DrawAt`; the normal map-mesh `Thing.Print` path ignores that override and can show the old gray/white smoke texture instead.
- Suicide bomber bomb vests currently render through the explicit `RenderExtras` overlay draw. A live 1.6 `zombieland/spawn_zombie` fixture plus `rimworld/screenshot_cell_rect` verified the black vest and red bomb overlay on a standing suicide bomber; latest screenshot evidence after removing the old fake-apparel hook is in `~/Library/Application Support/RimWorld/Screenshots/zl_suicide_bomber_overlay_raised_altitude__cell_rect.png`. The overlay altitude must stay above the pawn body, because the old `PawnGraphicSet.ResolveApparelGraphics` hook was removed so `RenderExtras` is the single map-rendering path until a render-node-native replacement exists.

## Known Risks

- RimWorld 1.6 pawn rendering was significantly refactored. Any Harmony patch touching pawn graphics should be treated as suspicious even after it compiles.
- RimWorld 1.6 pawn pathing can leave `Pawn_PathFollower.curPath` null while an async `curPathRequest` is pending. Zombieland movement code must not treat `curPath == null` as an immediately invalid destination when the pather is still moving toward the same cell; doing so caused `ZombieTest` zombies to move one cell and then jitter in place.
- `Source/ZombieRenderCompat.cs` is a compile-enabling compatibility layer for old `PawnRenderer.graphics` usage. Custom body/head assignment now re-resolves each zombie render tree after dirtying it, which fixes the one-frame `Attempted to draw <zombie> without a resolved render tree` errors on `ZombielandVisualLineup` load. Do not reintroduce graphics preparation in `PawnRenderer.ParallelPreRenderPawnAt`; that path touches map pawn lists off the main thread.
- The suicide bomber overlay is visually restored, but it is still a compatibility workaround rather than a RimWorld 1.6 render-node-native implementation.
- The old `PathFinder.FindPathNow` `PathFinderNodeFast.knownCost` transpiler remains disabled and should stay disabled. RimWorld 1.6 zombie avoidance path costs are now applied through per-request `PathRequest.IPathGridCustomizer` snapshots and disposed after synchronous `FindPathNow` calls, `PathRequest.Resolve`, or `PathRequest.Dispose` for async requests.
- `ZombieWanderer.processor` is a static coroutine that can keep running across yields while RimWorld starts a long-event save load. It must stay guarded by `LongEventHandler.AnyEventNowOrWaiting` / `ShouldWaitForEvent` and by per-map validity checks; without those guards, loading a save from an already-loaded Zombieland map can crash in `MapInfo.ValidFloodCell -> PathGrid.WalkableFast`.
- Compile success is not runtime success. The first runtime phase must validate mod load, map start, zombie spawning, pawn rendering, infection, corpse conversion, and special zombie behaviors.
- Generated logs and build output can consume context quickly. Keep artifacts on disk and summarize.
- `zombieland/spit_zombie_ball` covers the spitter job-driver launch path, `zombieland/zombie_ball_in_flight` covers source-derived projectile travel and rotation, and `zombieland/impact_zombie_ball` covers the custom impact effect.

## Fix Suggestions

### Unity `Originals/Effects` ZombieBlob Pink Material

Status: applied source-side in this repo and import-checked with Unity `2022.3.62f3` on macOS/Metal. Bundle export is now enabled through `CreateAssetBundles.EnsureGeneratedBundleAssets`, which reconstructs the missing `_Zombieland` bundle inputs before each export.

Problem:

- `Originals/Effects/Assets/Zombieland/ZombieBlob.shader` was authored for the old Unity project and uses `StructuredBuffer<float2> _Positions` plus `_Positions.Length`.
- Unity 2022's Metal compiler rejects buffer-size queries from shader code: pass a count explicitly or avoid the query.
- The tracked Unity YAML already contains old serialized GUID references, but this repo ignores and does not track `.meta` files. Unity therefore generates new GUIDs when opening the project, breaking the scene -> material and material -> shader links. The visible symptom is `ZombieBlob` rendering pink/magenta.

Applied minimal fix:

1. Patch `Originals/Effects/Assets/Zombieland/ZombieBlob.shader` only in the small preview/test path:
   - Uncomment the existing static `cellCount` and `positions[cellCount]` array.
   - Remove or stop using `StructuredBuffer<float2> _Positions`.
   - Replace the `_Positions.Length` loop with `for(int idx = 0; idx != cellCount; ++idx)`.
   - Replace `pos = _Positions[idx];` with `pos = positions[idx];`.

   Expected source diff shape:

   ```diff
   -         /*
             static const int cellCount = 18;
             static const float2 positions[cellCount] =
             {
                 ...
             };
   -         */
   -
   -         StructuredBuffer<float2> _Positions;

   ...
   -            // for(int idx = 0; idx != cellCount; ++idx)
   -            int count = (int)_Positions.Length - 1;
   -            for(int idx = 0; idx != count; ++idx)
   +            for(int idx = 0; idx != cellCount; ++idx)
                {
   -               pos = _Positions[idx]; // positions[idx];
   +               pos = positions[idx];
                   power += cellPower(idx, coord, pos);
                }
   ```

2. Add `Originals/Effects/Assets/Zombieland/ZombieBlob.mat.meta` with the historical material GUID used by `Effects.unity`:

   ```yaml
   fileFormatVersion: 2
   guid: 450cf6f31c7c57b48b4247fb7d2cd73f
   NativeFormatImporter:
     externalObjects: {}
     mainObjectFileID: 0
     userData:
     assetBundleName:
     assetBundleVariant:
   ```

3. Add `Originals/Effects/Assets/Zombieland/ZombieBlob.shader.meta` with the historical shader GUID used by `ZombieBlob.mat`:

   ```yaml
   fileFormatVersion: 2
   guid: c1a5c4f8208d05b498f00011d22dc916
   ShaderImporter:
     externalObjects: {}
     defaultTextures: []
     nonModifiableTextures: []
     userData:
     assetBundleName:
     assetBundleVariant:
   ```

4. Added `Originals/Effects/Assets/Zombieland/Rimworld.png.meta` with GUID `d75ba6bd32be5d34c888c16a0b983109`, because `Rimworld.mat` already references that texture GUID. This fixes the Rimworld material texture binding but is not required for `ZombieBlob` to render.

Avoid:

- Do not edit `Effects.unity`, `ZombieBlob.mat`, or `Rimworld.mat`; their serialized references already match the historical GUIDs.
- Do not add `Effects.unity.meta` or `Rimworld.mat.meta`; no tracked asset references those GUIDs.
- Do not broaden `.gitignore` unless there is a separate decision to start tracking Unity metas generally. For this targeted fix, force-add only the required `.meta` files because `.gitignore` currently ignores `*.meta`:

  ```bash
  git add -f Originals/Effects/Assets/Zombieland/ZombieBlob.mat.meta \
    Originals/Effects/Assets/Zombieland/ZombieBlob.shader.meta
  ```

Verification used in temp project:

- After adding the matching metas and restarting Unity, `/Users/ap/Desktop/Effects` rendered green blob shapes in the Scene view instead of a magenta plane.
- Before restarting Unity, the editor asset database still cached generated GUIDs, so a normal refresh was not enough. Restart Unity after changing these `.meta` GUIDs.
- The repo copy was opened from a temporary copy with Unity `2022.3.62f3` using `-batchmode -quit`; Unity exited successfully and did not report shader compiler errors for `ZombieBlob.shader`. The expected historical GUIDs were present in the temporary imported project.
- Current deployed bundle validation loads all expected assets from each platform bundle: `Dust`, `Metaballs`, `smoke_n.png`, `smoke_thin.mat`, `smoke_thin.png`, `zombieblob.mat`, and `zombieblob.shader`.
- Current deployed bundle inspection shows `Dust` is a billboard `ParticleSystem` prefab using `Smoke_thin` with Unity's built-in `Particles/Standard Surface` shader on Win64/Linux/MacOS. The deployed custom shaders are `Custom/Metaballs` and `Custom/ZombieBlob`; there is no separate custom thumper shader in the deployed bundle.
- A safe temp export using `ZOMBIELAND_RESOURCES_DIR` outside the repo now succeeds and validates all three platform bundles. The tracked `Resources` bundles were also validated after replacement, inspected with `scripts/inspect-unity-assets.sh`, confirmed in the deployed mod path by hash, loaded through GABS into `ZombieTest`, and smoke-tested with `zombieland/spawn_blob` plus `zombieland/thumper_impact_cycle`.
